#!/usr/bin/perl -w

use warnings;
use strict;
use RennPoints::MyLaps;
use Data::Dumper;

my $mylaps = RennPoints::MyLaps->new();
my $events = $mylaps->events;

if ( $events ) {
    print Dumper( $events );
}
